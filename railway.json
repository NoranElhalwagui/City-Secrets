{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd CitySecrets/CitySecrets && dotnet restore && dotnet publish -c Release -o out"
  },
  "deploy": {
    "startCommand": "cd CitySecrets/CitySecrets && dotnet out/CitySecrets.dll",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
